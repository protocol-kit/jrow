global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Jaeger metrics
  - job_name: 'jaeger'
    static_configs:
      - targets: ['jaeger:14269']

  # Add your JROW application metrics endpoints here
  # - job_name: 'jrow-server'
  #   static_configs:
  #     - targets: ['host.docker.internal:8080']

